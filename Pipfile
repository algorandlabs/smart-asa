[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
docopt = "*"
py-algorand-sdk = "*"
# TODO: switch to stable `pyteal` once the PyTeal ABI is released
pyteal = {git = "https://github.com/algorand/pyteal.git", ref = "feature/abi", editable = true}

[dev-packages]
autoflake = "*"
black = "*"
pytest = "*"

[requires]
python_version = "3.10"
